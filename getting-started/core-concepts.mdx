---
title: Core Concepts
description: Vouchers, sessions, settlement, and the Bleepay execution flow.
icon: lightbulb
---

# Core Concepts

Understanding these concepts will help you integrate and operate Bleepay correctly.

## Voucher (6-digit code)

A **voucher** is a short-lived, human-readable code generated by the payer’s wallet and bound to an authorization context. It acts as an **ephemeral proxy** for payment authorization.

- **Format**: Typically 6 digits (numeric) or 6 alphanumeric characters, depending on mode.
- **Transmission**: Can be passed by any channel: typed, spoken, QR, NFC, or messaging.
- **Lifecycle**: Reserved at generation, unreserved when the session is completed or expired.
- **Security**: Does not expose public or private keys; only the payer’s wallet can sign the resulting transaction.

## Session

A **voucher session** is the short-lived state on Bleepay’s side that links:

- The voucher code
- The payer’s identity (bound cryptographically)
- Transaction parameters (amount, currency, destination, etc.)

The session exists from voucher generation until the transaction object is retrieved and authorized by the payer. After that, the session is finalized and the voucher code is returned to the pool.

## Payment flow (high level)

1. **Merchant** starts a payment session via the SDK (amount, currency, IBAN, webhook, order_id).
2. **User** generates a 6-digit code in their wallet → creates a secure link between their assets and the merchant’s intent.
3. **User** enters the code in the merchant’s checkout (or the merchant’s system submits it).
4. **Bleepay** validates the voucher, syncs transaction metadata, and builds the transaction (swap/bridge/transfer) for the user’s chain and token.
5. **User** reviews and signs in their wallet; the wallet broadcasts to the liquidation partner’s infrastructure.
6. **Liquidation partner** converts crypto to fiat and sends funds to the merchant’s IBAN.
7. **Bleepay** sends a webhook to the merchant so they can finalize the order.

## Non-custodial model

- **Client-side signing**: Private keys stay in the user’s wallet. Bleepay provides transaction payloads; all signatures happen locally.
- **No custody**: Bleepay never holds user funds. Settlement is on-chain or via regulated off-ramp partners.
- **Session integrity**: Codes are ephemeral and bound to a specific payment intent, reducing replay and misuse.

## Chain-agnostic merchant experience

Merchants integrate using **financial metadata only**: amount, currency, IBAN, webhook, order_id. They do not:

- Choose chains or tokens
- Manage gas or RPC
- Write or deploy smart contracts

Bleepay’s backend handles routing, gas, cross-chain, and DEX logic so the merchant stays chain-agnostic.

## Next steps

- [Execution flow](/architecture/execution-flow) — detailed technical flow.
- [Terminology](/reference/terminology) — full glossary.
- [Protocol flow](/reference/protocol-flow) — voucher generation, redemption, and settlement steps.
